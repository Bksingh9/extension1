# Failure Log

Scan and research read this before processing new markets so the system does
not repeat past mistakes. `scripts/compound.py` appends entries automatically
after losing trades.

Format: `- YYYY-MM-DD <market_id> **<class>** pred=.. outcome=.. pnl=$.. — note`

Failure classes: `bad_prediction`, `bad_timing`, `bad_execution`, `external_shock`.

_(no entries yet)_

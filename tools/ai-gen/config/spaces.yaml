# Verified-live free Hugging Face Spaces (ZeroGPU).
#
# Format:
#   <kind>:
#     - id: <hf-user>/<space-name>
#       endpoint: null            # filled by `gen.py probe` on first use
#       fn: null                  # API function name (e.g. "/predict")
#       prompt_arg: null          # name of the prompt parameter
#       extra: {}                 # default values for non-prompt args
#       note: free text
#
# Re-rank / add / remove freely; gen.py reads this every run.

video:
  - id: Lightricks/ltx-video-distilled
    note: "Fast LTX-Video distilled; primary T2V pick"
  - id: Lightricks/LTX-2-3
    note: "Higher-quality LTX-2 (slower queue)"
  - id: multimodalart/wan2-1-fast
    note: "Wan 2.1 fast pipeline"
  - id: zerogpu-aoti/wan2-2-fp8da-aoti-faster
    note: "Wan 2.2 14B FP8DA"
  - id: Wan-AI/Wan2.1
    note: "Official Wan2.1"

image:
  - id: black-forest-labs/FLUX.1-dev
    note: "Flux dev — best quality, gated; you must accept the model card licence on HF"
  - id: evalstate/flux1_schnell
    note: "Flux schnell, Apache 2.0; primary T2I pick"
  - id: black-forest-labs/FLUX.2-dev
    note: "Flux.2 dev (if available in your region)"
  - id: multimodalart/flux-lora-the-explorer
    note: "Flux + community LoRAs"
